If I start again, what will I do instead?
1. Implement use type annotations and function docs from the start
2. Use logging instead of print (for exceptions, line numbers etc)
3. Seperate the discord bot and agent logic (this seems obvious)